{
  "name": "@villehx/aws-xray-sdk-layer",
  "version": "1.0.0",
  "main": "./dist/package/layer.js",
  "types": "./dist/package/layer.d.ts",
  "scripts": {
    "build": "npm run compile && npm run bundle",
    "compile": "tsc",
    "bundle": "esbuild --bundle --platform=node layer.ts --outdir=dist/layer/nodejs/node_modules"
  },
  "dependencies": {
    "aws-xray-sdk": "^3.4.1"
  }
}
